<% @lessons.each do |lesson| %>
  <div class="card-classes" style="background-image: url('<%= cl_image_path lesson.user.photo.key, width: 376, height: 460, crop: :fill, gravity: :face%>');" >
    <div>
      <p class="text-white fw-bold fs-5"><%= lesson.category %></p>
    </div>
    <div>
      <p class="text-white fw-bold fs-5"><%= lesson.user.name %></p>
    </div>
    <%= link_to lesson_path(lesson), class: "text-decoration-none text-reset" do %>
      <div>
        <span class="card-classes-button"> View Class → </span>
      </div>
    <% end %>
  </div>
<% end %>
